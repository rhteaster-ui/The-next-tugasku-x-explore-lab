import { PageStub } from "@/components/tugasku/PageStub";

export default function NotesPage() {
  return (
    <PageStub
      eyebrow="TugasKu · Catatan"
      title="Catatan"
      body="Catatan cepat dengan markdown ringan + tag. Local-first via IndexedDB; tabel notes di Supabase untuk versi cloud."
    />
  );
}
