import { PageStub } from "@/components/tugasku/PageStub";

export default function CalendarPage() {
  return (
    <PageStub
      eyebrow="TugasKu · Kalender"
      title="Kalender"
      body="Tampilan kalender bulanan + integrasi Google Calendar opt-in. Aktifkan integrasi di Settings → Integrasi untuk reminder yang tetap aktif meski web app tidak dibuka."
    />
  );
}
